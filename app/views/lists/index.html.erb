<h1>Hello</h1>

<ul>
<% @lists.each do |list| %>
<li class="list-item"> <%= link_to list.name, list_path(list) %>
</li>
<% end %>
<%# # shows the list, add a clicking link %>

</ul>

<%= link_to "Create a new List", new_list_path(@list) %>
