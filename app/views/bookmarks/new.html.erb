<h1> ADD A BOOKMARK : </h1>

<%= simple_form_for [ @list, @bookmark ] do |f| %>
  <%= f.input :comment %>
   <%= f.association :movie %>
  <%= f.submit "Submit Bookmark", class: "btn btn-primary" %>
<% end %>

<%= link_to "Back to Homepage", lists_path(@list) %>
